const locationButtons =
  document.querySelectorAll(".centers-grid .btn");

locationButtons.forEach(button => {

  button.addEventListener("click", () => {

    alert(
      "Map and GPS functionality will be connected later."
    );

  });

});