const buyButtons =
  document.querySelectorAll(".product-card .btn");

buyButtons.forEach(button => {

  button.addEventListener("click", () => {

    alert(
      "Shopping functionality will be connected later."
    );

  });

});